{
  "name": "@moregreen/db",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical Prisma schema + client for the More Green platform.",
  "scripts": {
    "validate": "prisma validate",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "deploy": "prisma migrate deploy"
  },
  "prisma": {
    "schema": "schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1"
  },
  "devDependencies": {
    "prisma": "^6.2.1"
  }
}
