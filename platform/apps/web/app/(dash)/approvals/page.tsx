import { ScreenStub } from "../_components/ScreenStub";

export default function ApprovalsPage() {
  return (
    <ScreenStub
      title="Approvals"
      description="Pending proposals grouped by type with current→proposed and projected ROAS; one-tap approve/reject; applied/failed history and cap-breach badges."
      task="E3-T4"
    />
  );
}
