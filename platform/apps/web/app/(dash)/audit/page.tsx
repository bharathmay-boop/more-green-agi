import { ScreenStub } from "../_components/ScreenStub";

export default function AuditPage() {
  return (
    <ScreenStub
      title="Audit"
      description="Append-only audit log of every state-changing action (who/what/when/before→after) with filters."
      task="E9"
    />
  );
}
